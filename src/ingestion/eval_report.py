"""Evaluation report parser.

Loads structured evaluation reports (JSON, YAML, or markdown) into
NormalisedArtifact instances. Planned for later implementation.

See CLAUDE.md: "Add eval_report.py (later)"
"""

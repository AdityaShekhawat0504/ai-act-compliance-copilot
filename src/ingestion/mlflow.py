"""MLflow run artifact parser.

Loads MLflow experiment runs (metrics, parameters, artifacts) into
NormalisedArtifact instances. Planned for later implementation.

See CLAUDE.md: "Add mlflow.py (later)"
"""
